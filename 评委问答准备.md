# 评委问答准备（5 分钟问答 · 高频问题与应答要点）

> 针对五大挑战 + 评分标准，预判评委问题并准备应答。答法要诀：先给结论，再给依据，最后落到"可落地"。

---

## 一、关于"Agent"定位（最可能问）

**Q1：你们这到底是大模型应用还是 Agent？和普通 AI 问答有什么区别？**
答：核心区别在"**任务执行**"。普通问答只给答案，我们的 Agent 会实际执行任务——生成申报单、发送邮件、出具自证声明。它具备"感知→决策→执行→优化"的完整闭环。而且我们不依赖大模型也能跑（规则引擎兜底），这是"可落地"的关键。

**Q2：你们用了哪个大模型？**
答：架构上是**可插拔**的——支持 OpenAI、DeepSeek、通义千问、本地 Ollama。当前演示环境走的是"规则引擎 + 本地检索"路线，零 API 成本也能完整运行；接入大模型后可进一步提升语义理解能力。

## 二、关于准确性/幻觉（对应"模型幻觉"挑战）

**Q3：AI 会误判吗？准确率多少？怎么保证不坑了员工？**
答：会有误判可能，所以我们做了三重保障：一是 **RAG 约束**，每个结论都引用题库案例作为依据，不是凭空生成；二是**规则引擎兜底**，大模型解析失败自动回退规则结果；三是**高风险必填披露**，宁可多申报，不漏报。测试上，8 类行为分类 + 边界情况（无关输入返回"不确定"）均验证通过。

**Q4：怎么处理大模型幻觉？**
答：从架构上抑制——温度参数 0.3 保证输出稳定；以检索案例为上下文约束；JSON 解析失败回退规则引擎。等于给大模型"圈定范围、给出依据、失败有兜底"。

## 三、关于数据安全（对应"数据安全"挑战）

**Q5：员工行为描述很敏感，数据安全怎么保证？**
答：我们做的是**离线优先**架构——核心判断逻辑在浏览器本地运行，行为描述不出本机；历史记录只存在本地 IndexedDB；邮件通过本地 SMTP 直发，不经过第三方平台。机构场景支持**私有化部署**，数据完全内网流转。

**Q6：230 道题库有版权问题吗？**
答：题库是 CFA Institute 官方练习题整理，用于教学与研究目的。商用阶段我们会取得授权，或用自有语料替换扩展。

## 四、关于可解释性（对应"可解释性"挑战）

**Q7：你们的判断是"黑箱"吗？员工凭什么信？**
答：不是黑箱。每个风险评级都附带"**涉及的准则条款 + 引用的题库案例**"，评分由准则映射 + 案例加权可追溯。员工能看到"为什么判高风险、依据是哪条、参考哪个案例"，这是可解释性的具体落地。

## 五、关于权限与责任（对应"权限管理/责任边界"挑战）

**Q8：权限管理怎么做？**
答：申报是"员工提交 → 合规部门审阅"的留痕闭环，每份申报单有唯一编号 + 时间戳。后续可扩展角色权限与审批流（如多级审批、越权拦截）。

**Q9：AI 给错建议，出了事谁负责？**
答：我们明确 Agent 是"**辅助决策工具**"，不是决策主体。输出标注参考性质，高风险场景强制引导向合规部门报备，最终由人工确认后行动才生效。责任边界清晰：AI 提供依据，人做决策。

## 六、关于落地与商业（对应"实际落地能力"）

**Q10：和市面上已有的合规软件（如合规引擎）比，你们有什么不同？**
答：传统合规软件是"机构级、被动审查、面向合规部门"；我们是"**个人级、主动自检、面向一线员工**"，填补的是员工在行动前"该不该做、要不要报"的决策空白。而且轻量、离线、随开随用，不依赖复杂部署。

**Q11：商业模式怎么赚钱？落地可行性如何？**
答：三层产品——个人免费版引流，团队版（内网部署、批量分析、审计）订阅收费，定制版（接入机构内部规则）项目制。落地路径清晰：已有可运行 MVP，下一步找 1-2 家投资机构做私有化试点。

**Q12：为什么选 CFA 准则，而不是更广的法律法规？**
答：因为 CFA 准则是**可结构化的规则集合**，最适合 Agent 落地验证；而且题库数据权威、场景典型。把 CFA 准则跑通后，方法论可以复制到更广的合规域（软美元、GIPS、反洗钱等），这是我们"持续优化"的扩展方向。

## 七、关于技术细节（可能追问）

**Q13：RAG 具体怎么实现的？**
答：意图分类 → ChromaDB 向量检索 Top-5 案例 → 构建 Prompt（行为+准则+案例）→ 结构化输出 → 任务执行。嵌入层用离线 TF-IDF/Jieba，可选 sentence-transformers 升级。

**Q14：没网也能用是真的吗？**
答：真的。Service Worker + 本地规则引擎 + 内置案例库，断网后仍能完成自检、申报单生成。只有"真实发邮件"需要网络（走 SMTP）。

---

## 应答三原则（提醒队员）

1. **先结论后依据**：直接回答问题，再补依据，不绕弯。
2. **落到"可落地"**：每个回答收尾都强调"已实现/已测试/可部署"。
3. **不硬撑**：被问到没准备的细节，坦诚"这是后续优化方向"，再给替代思路，绝不瞎编。
